{
  "name": "@hubble/motherduck",
  "private": true,
  "scripts": {
    "lint": "uvx ruff check .",
    "format": "uvx ruff format .",
    "format:check": "uvx ruff format --check .",
    "typecheck": "node -e \"console.log('No typecheck step for MCP servers')\"",
    "build": "node -e \"console.log('Use pnpm docker:build for container images')\"",
    "docker:build": "docker build --platform linux/amd64 -t hubble-mcp-servers:dev .."
  },
  "devDependencies": {
    "@hubble/prettier-config": "workspace:^",
    "prettier": "^3.6.2"
  },
  "prettier": "@hubble/prettier-config"
}
