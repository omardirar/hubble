---
name: Deploy preview

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
      CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install deps
        run: npm ci --prefer-offline --no-audit --progress=false

      - name: Build Next
        run: npm run build

      - name: OpenNext build (generates .open-next)
        run: npx opennextjs-cloudflare build --yes

      - name: Set up Wrangler
        run: npm run --silent wrangler -- --version || npm i -D wrangler && npx wrangler --version

      - name: Prepare branch env
        run: |
          echo "BRANCH_NAME=${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}" >> $GITHUB_ENV

      # Deploy Worker-level secrets from GitHub Secrets (non-interactive)
      - name: Set Worker secrets (preview)
        if: env.CLOUDFLARE_API_TOKEN && env.CLOUDFLARE_ACCOUNT_ID
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          printf "%s" "${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}" | \
            npx wrangler secret put NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY --env preview --yes
          printf "%s" "${CLERK_SECRET_KEY}" | \
            npx wrangler secret put CLERK_SECRET_KEY --env preview --yes
          printf "%s" "${ANTHROPIC_API_KEY}" | \
            npx wrangler secret put ANTHROPIC_API_KEY --env preview --yes
          printf "%s" "${SUPABASE_URL}" | \
            npx wrangler secret put SUPABASE_URL --env preview --yes
          printf "%s" "${SUPABASE_ANON_KEY}" | \
            npx wrangler secret put SUPABASE_ANON_KEY --env preview --yes

      - name: Deploy to Cloudflare (preview)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          npx wrangler deploy --env preview \
            --var GIT_COMMIT=${{ github.sha }} \
            --var GIT_BRANCH=${BRANCH_NAME}
