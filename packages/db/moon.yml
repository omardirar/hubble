$schema: "https://moonrepo.dev/schemas/project.json"

id: "db"
language: "typescript"
layer: "library"
stack: "backend"
tags:
  - "shared"

dependsOn:
  - "config"

workspace:
  inheritedTasks:
    exclude: ["build"]

fileGroups:
  sources:
    - "src/**/*"
