$schema: "https://moonrepo.dev/schemas/project.json"

language: "typescript"
layer: "library"
stack: "backend"
tags:
  - "shared"

dependsOn:
  - "core"
  - "db"
  - "schemas"
  - "config"
  - "auth"
  - "logger"
  - "infrastructure"

workspace:
  inheritedTasks:
    exclude: ["build"]

fileGroups:
  sources:
    - "src/**/*"
