$schema: "https://moonrepo.dev/schemas/project.json"

id: "logger"
language: "typescript"
layer: "library"
stack: "backend"
tags:
  - "shared"

workspace:
  inheritedTasks:
    exclude: ["build"]

fileGroups:
  sources:
    - "src/**/*"
