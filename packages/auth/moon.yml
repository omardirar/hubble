$schema: "https://moonrepo.dev/schemas/project.json"

id: "auth"
language: "typescript"
layer: "library"
stack: "backend"
tags:
  - "shared"

dependsOn:
  - "core"
  - "logger"
  - "db"

workspace:
  inheritedTasks:
    exclude: ["build"]

fileGroups:
  sources:
    - "src/**/*"
