$schema: "https://moonrepo.dev/schemas/project.json"

language: "javascript"
layer: "configuration"
stack: "infrastructure"
tags:
  - "shared"
  - "config"

workspace:
  inheritedTasks:
    exclude: ["lint", "typecheck", "format", "format-check"]

fileGroups:
  sources:
    - "*.cjs"
    - "*.json"

tasks:
  build:
    command: "echo"
    args:
      - "No build needed for prettier-config package"
    options:
      cache: true
      runInCI: true
      mergeArgs: "replace"
      mergeDeps: "replace"
      mergeOutputs: "replace"
