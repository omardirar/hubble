$schema: "https://moonrepo.dev/schemas/project.json"

id: "types"
language: "typescript"
layer: "library"
stack: "backend"
tags:
  - "shared"

dependsOn:
  - "schemas"

workspace:
  inheritedTasks:
    exclude: ["build"]

fileGroups:
  sources:
    - "src/**/*"
