$schema: "https://moonrepo.dev/schemas/project.json"

id: "schemas"
language: "typescript"
layer: "library"
stack: "backend"
tags:
  - "shared"

workspace:
  inheritedTasks:
    exclude: ["build"]

fileGroups:
  sources:
    - "src/**/*"
