$schema: "https://moonrepo.dev/schemas/project.json"

language: "typescript"
layer: "configuration"
stack: "infrastructure"
tags:
  - "shared"
  - "config"

workspace:
  inheritedTasks:
    exclude: ["lint", "typecheck", "format", "format-check", "build"]

fileGroups:
  sources:
    - "*.json"
