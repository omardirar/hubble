[project]
name = "hubble-mcp-servers"
version = "1.0.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[project.optional-dependencies]
dev = ["ruff>=0.13.3", "mypy>=1.18.0"]

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
